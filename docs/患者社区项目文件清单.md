# 患者社区项目文件清单

## 📦 交付文件说明

### 1. patient-community-full.tar.gz (57KB)
**完整项目源代码压缩包**

包含内容：
- 前端Vue 3源代码（不含node_modules和构建产物）
- 后端FastAPI源代码（不含Python缓存）
- 数据库文件（SQLite）
- 所有配置文件
- 文档文件

解压命令：
```bash
tar -xzf patient-community-full.tar.gz
```

### 2. frontend-dist.tar.gz (约1.5MB)
**前端生产构建产物**

包含内容：
- 已构建的前端静态文件
- 压缩优化后的JS和CSS
- 可直接部署到Web服务器

解压命令：
```bash
tar -xzf frontend-dist.tar.gz
```

### 3. patient_community.db (约100KB)
**SQLite数据库文件**

包含内容：
- 用户表（含测试账号）
- 病历表
- 社区表
- 帖子和评论表
- 治疗方案表

直接使用：
```bash
# 查看数据库内容
sqlite3 patient_community.db
.tables
.schema users
```

### 4. nginx.conf
**Nginx配置文件**

用途：
- 配置反向代理
- 静态文件服务
- API路由

使用方法：
```bash
sudo cp nginx.conf /etc/nginx/sites-available/patient-community
sudo ln -s /etc/nginx/sites-available/patient-community /etc/nginx/sites-enabled/
sudo nginx -t
sudo nginx -s reload
```

## 📁 项目目录结构

```
patient-community/
├── frontend/                      # 前端项目
│   ├── src/                       # 源代码
│   │   ├── views/                 # 页面组件（9个）
│   │   ├── api/                   # API接口（4个）
│   │   ├── stores/                # 状态管理
│   │   ├── router/                # 路由配置
│   │   ├── utils/                 # 工具函数
│   │   ├── assets/                # 静态资源
│   │   ├── App.vue                # 根组件
│   │   └── main.js                # 入口文件
│   ├── package.json               # 依赖配置
│   ├── vite.config.js             # Vite配置
│   └── index.html                 # HTML模板
│
├── backend/                       # 后端项目
│   ├── app/
│   │   ├── api/                   # API路由（4个模块）
│   │   │   ├── auth.py            # 认证路由
│   │   │   ├── records.py         # 病历路由
│   │   │   ├── match.py           # 匹配路由
│   │   │   └── community.py       # 社区路由
│   │   ├── models/                # 数据模型（3个）
│   │   │   ├── user.py            # 用户模型
│   │   │   ├── medical_record.py  # 病历模型
│   │   │   └── community.py       # 社区模型
│   │   ├── schemas/               # Pydantic模型
│   │   │   ├── user.py
│   │   │   └── medical_record.py
│   │   ├── services/              # 业务逻辑
│   │   │   ├── ai_service.py      # AI提取服务
│   │   │   └── match_service.py   # 匹配算法
│   │   ├── utils/                 # 工具函数
│   │   │   ├── security.py        # 安全工具
│   │   │   └── file_handler.py    # 文件处理
│   │   ├── config.py              # 配置文件
│   │   ├── database.py            # 数据库连接
│   │   └── main.py                # 应用入口
│   ├── patient_community.db       # SQLite数据库
│   ├── init_db.py                 # 数据库初始化
│   └── requirements.txt           # Python依赖
│
├── nginx.conf                     # Nginx配置
├── README.md                      # 项目说明
├── DEPLOYMENT.md                  # 部署指南
├── 患者社区生产环境部署说明.md    # 生产部署说明
└── PROJECT_SUMMARY.md             # 项目清单
```

## 🚀 快速部署指南

### 方式一：使用源代码部署

1. **解压项目**
```bash
tar -xzf patient-community-full.tar.gz
cd patient-community
```

2. **安装前端依赖**
```bash
cd frontend
pnpm install
# 开发模式
pnpm dev
# 或构建生产版本
pnpm build
```

3. **安装后端依赖**
```bash
cd ../backend
pip install -r requirements.txt
```

4. **初始化数据库**
```bash
python init_db.py
```

5. **启动后端**
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 方式二：使用构建产物部署

1. **解压前端构建产物**
```bash
tar -xzf frontend-dist.tar.gz
```

2. **配置Nginx**
```bash
sudo cp nginx.conf /etc/nginx/sites-available/patient-community
sudo ln -s /etc/nginx/sites-available/patient-community /etc/nginx/sites-enabled/
sudo nginx -t
sudo nginx -s reload
```

3. **启动后端**
```bash
cd patient-community/backend
uvicorn app.main:app --host 127.0.0.1 --port 8000
```

## 🔧 环境要求

### 前端
- Node.js 18+
- pnpm 或 npm

### 后端
- Python 3.11+
- pip

### 生产环境
- Nginx 1.18+
- MySQL 8.0+（可选，默认使用SQLite）

## 📝 配置说明

### 前端配置
文件：`frontend/vite.config.js`
- 开发端口：3000
- API代理：/api -> http://localhost:8000

### 后端配置
文件：`backend/app/config.py`
- 数据库：SQLite（可切换MySQL）
- JWT密钥：需要在生产环境修改
- OpenAI API Key：需要配置环境变量

### Nginx配置
文件：`nginx.conf`
- 监听端口：80
- 静态文件：frontend/dist
- API代理：http://127.0.0.1:8000

## 🔐 安全提示

1. **修改密钥**
   - 修改 `backend/app/config.py` 中的 `SECRET_KEY`
   - 使用强密码策略

2. **配置HTTPS**
   - 使用Let's Encrypt免费证书
   - 配置SSL/TLS

3. **数据库安全**
   - 生产环境切换到MySQL
   - 定期备份数据库
   - 限制数据库访问权限

4. **API安全**
   - 配置CORS白名单
   - 添加API速率限制
   - 启用请求日志

## 📊 数据库说明

### 测试数据
- 测试账号：testuser / 123456
- 5个疾病社区
- 2个治疗方案示例

### 数据表
- users：用户表
- medical_records：病历表
- medical_info：病历信息表
- communities：社区表
- posts：帖子表
- comments：评论表
- treatment_solutions：治疗方案表

### 数据库工具
```bash
# 查看数据库
sqlite3 patient_community.db

# 导出数据
sqlite3 patient_community.db .dump > backup.sql

# 导入数据
sqlite3 patient_community.db < backup.sql
```

## 🌐 在线演示

**网站地址**: https://80-i9vsk9cwbog8xzwzo460r-0ecdc938.sg1.manus.computer

**测试账号**: testuser / 123456

## 📞 技术支持

### 文档
- README.md：项目介绍和快速开始
- DEPLOYMENT.md：详细部署指南与本地开发
- 患者社区生产环境部署说明.md：生产环境部署（Nginx、服务管理、监控）
- PROJECT_SUMMARY.md：完整功能清单与交付说明

### API文档
访问 `/docs` 查看自动生成的API文档

## 📄 许可证

MIT License

---

**打包日期**: 2026年2月2日  
**项目版本**: 1.0.0  
**文件总数**: 42个源代码文件  
**代码行数**: 约3000行
